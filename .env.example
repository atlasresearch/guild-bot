# Copy to .env and fill - do NOT commit real secrets to git
# Example placeholders — copy this file to `.env` and replace values
DISCORD_TOKEN=

# Optional: limit command registration to a specific guild during development
GUILD_ID=
RECORDING_TRANSCRIPT_CHANNEL_ID=

WHISPER_MODEL=

# Optional: if set, /ask will only work in this channel
CHANNEL_ID=
LLM_URL=

UNIVERSE=discord