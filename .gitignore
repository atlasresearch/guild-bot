node_modules/
dist/
csvs/
package-lock.json
.tmp*
.test*
.DS_Store
.sessions/
.lancedb_*
.lancedb

# secrets: defence in depth — secrets.json lives in <GUILD_DIR>/, not the repo
secrets.json
secrets.*.json
!secrets.example.json

# legacy .env files removed by plan 003; refuse to track new ones
.env
.env.*
