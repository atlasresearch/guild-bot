{
  "type": "function",
  "function": {
    "name": "tag_message",
    "description": "Add semantic tags to an indexed message for later filtering and search.",
    "parameters": {
      "type": "object",
      "properties": {
        "message_id": { "type": "string", "description": "Discord message snowflake ID" },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Tags to add"
        }
      },
      "required": ["message_id", "tags"]
    }
  }
}
